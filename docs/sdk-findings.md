# TrueForge SDK findings

Filled in by teammate A during discovery. Until this is answered, the collector
is guesswork.

| Question | Answer | Source |
|---|---|---|
| Session exposes per-step tokens? | | |
| Session exposes per-step timings? | | |
| SSE event types and timestamps | | |
| TTFT measurable from stream? | | |
| Subagent steps distinguishable? | | |
| Compaction observable as an event? | | |
| Capabilities settable per agent via SDK? | | |
| Gateway exposes raw prompt? | | |

## Gaps

Anything unavailable above, and which seam we get it from instead.
